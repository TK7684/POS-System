# Enhanced AI Agent - คู่มือการใช้งานฉบับสมบูรณ์

## 📋 สารบัญ

1. [ภาพรวม](#ภาพรวม)
2. [คุณสมบัติเด่น](#คุณสมบัติเด่น)
3. [การติดตั้งและการตั้งค่า](#การติดตั้งและการตั้งค่า)
4. [วิธีใช้งาน](#วิธีใช้งาน)
5. [รูปแบบข้อมูลที่รองรับ](#รูปแบบข้อมูลที่รองรับ)
6. [การจัดหมวดหมู่ข้อมูล](#การจัดหมวดหมู่ข้อมูล)
7. [API Reference](#api-reference)
8. [การแก้ไขปัญหา](#การแก้ไขปัญหา)
9. [FAQ](#faq)

## 📖 ภาพรวม

Enhanced AI Agent เป็นระบบปัญญาประดิษฐ์ที่พัฒนาขึ้นเพื่อช่วยในการจัดการข้อมูลค่าใช้จ่ายของร้านอาหารโดยเฉพาะ ระบบสามารถวิเคราะห์ข้อความจากบันทึกค่าใช้จ่าย แยกประเภทข้อมูลอัตโนมัติ และบันทึกลงในตารางที่เหมาะสมใน Google Sheets

### 🎯 วัตถุประสงค์

- ลดเวลาในการบันทึกข้อมูลค่าใช้จ่ายจากหลายชั่วโมงเป็นไม่กี่นาที
- ลดความผิดพลาดจากการป้อนข้อมูลด้วยตนเอง
- จัดระเบียบข้อมูลอย่างเป็นระบบ
- ให้ภาพรวมของค่าใช้จ่ายแบบ Real-time

## ✨ คุณสมบัติเด่น

### 🚀 ประมวลผลแบบกลุ่ม (Batch Processing)
- รองรับข้อมูลหลายรายการในครั้งเดียว
- วิเคราะห์ข้อมูลได้ถึง 100 รายการต่อครั้ง
- แสดงผลการวิเคราะห์ก่อนบันทึก

### 🎯 การจัดหมวดหมู่อัตโนมัติ
- **วัตถุดิบ**: กุ้ง พริก มะนาว ผักต่างๆ
- **ค่าแรง**: ค่าจ้างพนักงาน เงินเดือน
- **อุปกรณ์**: เครื่องพิมพ์ โทรศัพท์ ที่นอน
- **วัสดุสิ้นเปลือง**: ถุง กล่อง สติ๊กเกอร์
- **สาธารณูปโภค**: ค่าน้ำ ค่าไฟ น้ำแข็ง

### 🔍 การวิเคราะห์ขั้นสูง
- รู้จักรูปแบบวันที่ DD-MMM-YYYY (20-Sep-2025)
- รองรับการคูณจำนวน (*12, *2, *5)
- แยกแยะหน่วยวัดอัตโนมัติ (กิโล กรัม มิลลิลิตร)

### ✅ ระบบยืนยันข้อมูล
- แสดงผลการวิเคราะห์ให้ตรวจสอบก่อนบันทึก
- สามารถแก้ไขข้อมูลก่อนยืนยันได้
- แสดงสรุปจำนวนและมูลค่าตามประเภท

## 🛠️ การติดตั้งและการตั้งค่า

### 1. ดาวน์โหลดไฟล์ที่จำเป็น

```javascript
// Core files
/js/core/enhanced/AIAgentChatEnhanced.js
/js/core/enhanced/AIAgentChatEnhancedUI.js

// Backend
/gas/enhanced-functions.gs

// Test page
/test-ai-agent-enhanced.html
```

### 2. เพิ่มลงใน Index.html

```html
<!-- Enhanced AI Agent CSS & JS -->
<link rel="stylesheet" href="css/ai-chat.css">
<script src="js/core/enhanced/AIAgentChatEnhanced.js" defer></script>
<script src="js/core/enhanced/AIAgentChatEnhancedUI.js" defer></script>
```

### 3. ตั้งค่า Google Apps Script

เพิ่มไฟล์ `enhanced-functions.gs` ลงในโปรเจค Google Apps Script ของคุณ

## 📖 วิธีใช้งาน

### การเปิดใช้งาน AI Agent Enhanced

1. คลิกที่ปุ่ม **🤖+** ด้านล่างขวาของหน้าจอ
2. ระบบจะแสดงหน้าต่าง AI Agent Enhanced
3. เลือกโหมด **ประมวลผลกลุ่ม** สำหรับข้อมูลหลายรายการ

### การประมวลผลข้อมูลแบบกลุ่ม

```
ขั้นตอนที่ 1: วางข้อความรายการค่าใช้จ่าย
ขั้นตอนที่ 2: คลิก "🔍 วิเคราะห์ข้อมูล"
ขั้นตอนที่ 3: ตรวจสอบผลการจัดหมวดหมู่
ขั้นตอนที่ 4: กด "✅ ยืนยันการบันทึก"
```

### การใช้งานโหมดแชท

สำหรับคำสั่งเดี่ยวหรือการสอบถามข้อมูล:

- "ช่วยบันทึก กุ้งสด 3 กิโล 450 บาท"
- "สต๊อกวัตถุดิบทั้งหมด"
- "คำนวณต้นทุนเมนูกุ้งแช่น้ำปลา"

## 📝 รูปแบบข้อมูลที่รองรับ

### รูปแบบวันที่

| รูปแบบ | ตัวอย่าง | สถานะ |
|---------|---------|--------|
| DD-MMM-YYYY | 20-Sep-2025 | ✅ รองรับ |
| YYYYMMDD | 20250920 | ✅ รองรับ |
| YYYY-MM-DD | 2025-09-20 | ✅ รองรับ |

### รูปแบบรายการ

```
[วันที่] [รายการ] [จำนวน] [หน่วย] [ราคา]
```

**ตัวอย่าง:**
```
20-Sep-2025	คริสตัล 600 มล *12	49
21-Sep-2025	เครื่องพิมพ์ใบเสร็จ *2	985
21-Sep-2025	ค่าจ้างน้องพลอย	300
```

### รูปแบบการคูณ

```
[รายการ] *[จำนวน]	[ราคา]
```

**ตัวอย่าง:**
```
คริสตัล 600 มล *12	49     → 12 ชิ้น @ 4.08 บาท
กล่องข้าวใหญ่ *2	398    → 2 กล่อง @ 199 บาท
ถุงขยะ 50ใบ *5	320.5  → 250 ถุง @ 1.28 บาท
```

## 🏷️ การจัดหมวดหมู่ข้อมูล

### หมวดหมู่หลัก

| ประเภท | ตาราง | ตัวอย่างรายการ |
|---------|--------|----------------|
| วัตถุดิบ | Purchases | กุ้ง พริก มะนาว ผัก |
| ค่าใช้จ่าย | Expenses | ค่าจ้าง ค่าน้ำ ค่าไฟ |
| อุปกรณ์ | Expenses (พิเศษ) | เครื่องพิมพ์ โทรศัพท์ ที่นอน |
| วัสดุสิ้นเปลือง | Expenses (พิเศษ) | ถุง กล่อง สติ๊กเกอร์ |

### กฎการจัดหมวดหมู่

1. **วัตถุดิบ**: คำที่เกี่ยวข้องกับอาหารและเครื่องปรุง
2. **ค่าแรง**: คำว่า "ค่าจ้าง" "จ้าง" "พนักงาน" "พลอย"
3. **อุปกรณ์**: คำว่า "เครื่อง" "โทรศัพท์" "ที่นอน" "หมอน"
4. **บรรจุภัณฑ์**: คำว่า "ถุง" "กล่อง" "ซอง" "ขวด"
5. **สาธารณูปโภค**: คำว่า "ค่าน้ำ" "ค่าไฟ" "น้ำแข็ง"

## 📚 API Reference

### Frontend Methods

#### `processBatchText(textInput)`
ประมวลผลข้อความแบบกลุ่ม

```javascript
const result = await aiAgent.processBatchText(text);
console.log(result.data); // ข้อมูลที่แยกประเภทแล้ว
```

#### `confirmAndSave(confirmed)`
ยืนยันและบันทึกข้อมูล

```javascript
const result = await aiAgent.confirmAndSave(true);
console.log(result.success); // true/false
```

### Backend Functions

#### `batchAddFromAI(params)`
เพิ่มข้อมูลหลายรายการพร้อมกัน

```javascript
google.script.run
  .batchAddFromAI({
    purchases: [...],
    expenses: [...],
    equipment: [...],
    supplies: [...]
  })
  .withSuccessHandler(console.log)
  .withFailureHandler(console.error);
```

#### `categorizeExpenseEnhanced(description)`
จัดหมวดหมู่รายการค่าใช้จ่าย

```javascript
const category = google.script.run
  .categorizeExpenseEnhanced("ค่าจ้างพนักงาน");
// ผลลัพธ์: { category: "ค่าแรง", type: "expense" }
```

## 🔧 การแก้ไขปัญหา

### ปัญหาที่พบบ่อย

#### 1. ไม่สามารถวิเคราะห์ข้อมูลได้
**สาเหตุ:** รูปแบบข้อมูลไม่ถูกต้อง
**วิธีแก้:**
- ตรวจสอบรูปแบบวันที่ (DD-MMM-YYYY)
- แน่ใจว่ามีราคาท้ายบรรทัดเสมอ
- ใช้ Tab หรือ ช่องว่างในการคั่นระหว่างข้อมูล

#### 2. จัดหมวดหมู่ผิด
**สาเหตุ:** คำในรายการตรงกับหลายหมวด
**วิธีแก้:**
- ตรวจสอบคำหลักในรายการ
- ใช้คำที่เฉพาะเจาะจงขึ้น
- แก้ไขหมวดหมู่หลังจากวิเคราะห์

#### 3. บันทึกข้อมูลไม่สำเร็จ
**สาเหตุ:** ไม่มีสิทธิ์ในการแก้ไข Google Sheets
**วิธีแก้:**
- ตรวจสอบการอนุญาตของ Google Apps Script
- ให้สิทธิ์ Editor ใน Google Sheets

### Debug Mode

เปิดดูข้อมูล debug ใน Console:

```javascript
// แสดงข้อมูลที่แยกแล้ว
console.log(window.aiChatEnhancedUI.aiAgent.pendingData);

// แสดงประวัติการสนทนา
console.log(window.aiChatEnhancedUI.conversationHistory);
```

## ❓ FAQ

### Q: รองรับภาษาอะไรบ้าง?
A: ปัจจุบันรองรับภาษาไทยเป็นหลัก และภาษาอังกฤษสำหรับรายการบางอย่าง

### Q: สามารถประมวลผลข้อมูลกี่รายการต่อครั้ง?
A: สูงสุด 100 รายการต่อครั้ง เพื่อประสิทธิภาพที่ดีที่สุด

### Q: สามารถแก้ไขข้อมูลหลังจากบันทึกได้ไหม?
A: ได้ สามารถแก้ไขได้โดยตรงใน Google Sheets

### Q: ข้อมูลจะถูกเก็บไว้ที่ไหน?
A: ข้อมูลถูกเก็บไว้ใน Google Sheets ของคุณเท่านั้น ปลอดภัยและเป็นส่วนตัว

### Q: ใช้งานออฟไลน์ได้ไหม?
A: ไม่ได้ ต้องเชื่อมต่ออินเทอร์เน็ตเพื่อบันทึกข้อมูล

## 📈 เวอร์ชันและการอัพเดท

### เวอร์ชัน 2.0 (ปัจจุบัน)
- ✅ ประมวลผลแบบกลุ่ม
- ✅ จัดหมวดหมู่อัตโนมัติ
- ✅ ระบบยืนยันข้อมูล
- ✅ รองรับหลายรูปแบบวันที่

### แผนการพัฒนา (v2.1)
- 📊 รายงานสรุปประจำเดือน
- 📱 รองรับ OCR จากรูปภาพ
- 🔄 ซิงค์ข้อมูลกับระบบอื่น
- 📈 กราฟแสดงแนวโน้มค่าใช้จ่าย

## 📞 การสนับสนุน

หากพบปัญหาหรือมีข้อเสนอแนะ:
1. ตรวจสอบในส่วน [การแก้ไขปัญหา](#การแก้ไขปัญหา) ก่อน
2. ติดต่อผู้พัฒนาผ่าน GitHub Issues
3. ดูตัวอย่างการใช้งานใน `test-ai-agent-enhanced.html`

---

**พัฒนาด้วย ❤️ สำหรับผู้ประกอบการร้านอาหารไทย**