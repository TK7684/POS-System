# Cloudflare Pages redirects
# Note: Functions in /functions folder are processed FIRST, before redirects
# SPA routing - redirect all non-function routes to index.html
# Function routes (/line-webhook, /midnight-stock-alert) are handled by functions/ folder
/*    /index.html   200

